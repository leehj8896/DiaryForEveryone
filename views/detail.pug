html
    head
        title 모두의 일기장
    body
        h1  
            a(href='/') 모두의 일기장
        table(border='1')
            tr
                th 제목
                td=post[0].title
            tr
                th 내용
                td=post[0].description
        button(onclick=`location.href='/update/${post[0].id}'`) 수정
        button(onclick=`location.href='/delete_process/${post[0].id}'`) 삭제
